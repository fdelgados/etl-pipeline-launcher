version: "3"

services:
  application:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      - LAUNCHER_DB_USER=launcher
      - LAUNCHER_DB_PASSWORD=wTUbtEmk2S6R
      - IDENTITYACCESS_DB_USER=master
      - IDENTITYACCESS_DB_PASSWORD=hY54t68WGWpd
      - API_HOST=localhost
      - FLASK_DEBUG=1
      - FLASK_ENV=development
    volumes:
      - ./src:/src
      - ./tests:/tests
      - ./config:/config
    ports:
      - "5000:5000"

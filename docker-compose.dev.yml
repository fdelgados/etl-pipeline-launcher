version: "3"

services:
  application:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      - DB_USER=launcher
      - DB_HOST=etl-pipeline-launcher-mysql
      - DB_PASSWORD=wTUbtEmk2S6R
      - API_HOST=localhost
      - FLASK_DEBUG=1
      - FLASK_ENV=development
    volumes:
      - ./src:/src
      - ./tests:/tests
      - ./config:/config
    ports:
      - "5000:5000"
